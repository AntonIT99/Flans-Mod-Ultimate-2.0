# Driveables

- Reuse the existing coordinate, collision, suspension, and interpolation helpers;
  do not introduce parallel physics or transform paths.
- Add tests for deterministic parsers and pure driveable logic where practical.
